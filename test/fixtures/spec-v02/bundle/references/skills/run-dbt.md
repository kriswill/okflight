---
type: Reference
title: Run dbt
description: Executor instructions for compiling and running a dbt model with bound vars.
---

`dbt run --vars` with the bound parameters; return `run_id`, the `compiled_sql`, and the `result`.
