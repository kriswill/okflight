---
type: Reference
title: Run on BigQuery
description: Executor instructions for running a bound SQL computation on BigQuery and returning a receipt.
---

Run the bound SQL as a job; return `job_id`, the `executed_sql` BigQuery reports, and the `result` rows.
